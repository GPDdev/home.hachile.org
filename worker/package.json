{
  "name": "home-discussions-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply home-discussions --local",
    "db:remote": "wrangler d1 migrations apply home-discussions --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
